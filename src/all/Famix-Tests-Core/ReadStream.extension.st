Extension { #name : #ReadStream }

{ #category : #'*Famix-Tests-Core' }
ReadStream >> converter: aConverter [
	"I am implementing because the tests use a memory store that return readStream instead of MultiByteFileStream..."

	
]
