converting
asXPathFilterExpressionLocationPathRoot
	"any node set can be used as the root of a filter expression
	location path, like (/one)/two or '$one/two' (when $one is bound
	to a node set)"

	^ self