operations - stubs
containingBehaviorStub
	""
	
	"{OCL} result = (if inStructuredNode<>null then inStructuredNode.containingBehavior() 
else if activity<>null then activity
else interaction 
endif
endif
)"
	
	self notYetImplemented
